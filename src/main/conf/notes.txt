# Building a jar with dependencies:
1. Click: "Run As > Maven build"
2. Type for "Goals": "clean package assembly:single"

Features:
- Extract exif image data -> external lib
- Setup versioning system and add project: -> SVN on NAS
- GUI -> JavaFX
- Build jar with maven
- Spring dependancy injection

Ideas:
- Test Coverage 100%
- Use FX XML